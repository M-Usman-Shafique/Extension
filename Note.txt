
I have created a basic chrome extension using React.JS (vite) and TailwindCSS to dynamically change both the background and text color of chrome webpages. Here's the basic files of my extension app:

The "nerd.png" image is inside the public folder.



- Use specific URL patterns in matches instead of <all_urls> to limit the extension's scope.
- Minimize calls to chrome.storage.local.get and consolidate related queries to reduce overhead.
- Show notifications or a status message when colors are applied, reset, or disabled for better user experience.
- welcome notification
- Consider excluding more elements such as script or style tags in addition to media elements.
- Consider handling cases where the storage query or chrome.tabs.sendMessage fails (e.g., closed tabs, inactive tabs).